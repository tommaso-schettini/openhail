# Experiment records

- `posthoc-20260909/`: controller timing measurements, execution metadata,
  summaries, source archive, and file manifests.
- `training-1455644/`: training summaries and iteration durations.

Pretrained weights and training configurations are in
[checkpoints/](../../checkpoints/). The source archive contains the implementation
used for the recorded timing experiment.

See the [reproduction guide](../../docs/reproducibility.md) for commands and
experiment settings.
